<Window x:Class="CitizenRegistry.Views.LogInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CitizenRegistry.ViewModels"
        xmlns:local="clr-namespace:CitizenRegistry.Views"
        mc:Ignorable="d"
        Title="Вход в базу" Height="450" Width="360" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:LogInViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button HorizontalAlignment="Left" Margin="10, 10, 0, 10" Command="{Binding ChoosePlaceForDB}" IsEnabled="{Binding IsCreateAvailable}" Content="Создать базу" Width="100"/>
        <PasswordBox x:Name="DatabasePasswordBox" IsEnabled="{Binding IsPasswordNeeded}" FontSize="19" Grid.Column="1" Margin="10" PasswordChanged="PasswordBox_PasswordChanged"></PasswordBox>
        <CheckBox IsChecked="{Binding IsPasswordNeeded}" Grid.Row="1" Grid.ColumnSpan="2" Content="С паролем" Margin="8.6, 0, 0, 0"/>
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Left" Orientation="Horizontal">
            <Button Margin="10, 0, 0, 10" Command="{Binding AddExistingDatabase}" IsEnabled="{Binding CanUseUI}" Content="Добавить базу" Width="100"/>
            <Button Margin="10, 0, 0, 10" Command="{Binding RenameDatabase}" IsEnabled="{Binding CanUseUI}" Content="Переименовать" Width="100"/>
        </StackPanel>
        <ListView HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" SelectedItem="{Binding SelectedDatabase}" ItemsSource="{Binding Databases}" Grid.Row="3" Grid.ColumnSpan="2" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <!-- Шаблон элемента с кнопками -->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock HorizontalAlignment="Stretch" Text="{Binding Name}" VerticalAlignment="Center" Padding="5" TextTrimming="CharacterEllipsis"/>
                        <Button Click="DatabaseItemRemoveButton_Click" Width="24" CommandParameter="{Binding .}" Grid.Column="1" Background="#960923" Content="✕" Foreground="White" Margin="1" ToolTip="Убрать"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Resources>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick"/>
                </Style>
            </ListView.Resources>
        </ListView>
    </Grid>
</Window>
