<Window x:Class="CitizenRegistry.Views.EditRecordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CitizenRegistry.Views"
        mc:Ignorable="d"
        Title="Редактирование записи" Height="550" Width="570" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="RecordBox" AcceptsReturn="True" FontSize="14" TextWrapping="Wrap" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="12, 12, 12, 0" ScrollViewer.VerticalScrollBarVisibility="Visible"></TextBox>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,10,0,0" HorizontalAlignment="Left">
            <TextBlock x:Name="CreatedDateBlock" FontSize="10" MinWidth="70" Margin="5,0,0,0"/>
            <TextBlock x:Name="ChangedDateBlock" FontSize="10" MinWidth="70" Margin="10,0,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <Button Click="SaveButton_Click" Content="Сохранить" Width="70"/>
            <Button Click="CancelButton_Click" Content="Отмена" Width="70" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
